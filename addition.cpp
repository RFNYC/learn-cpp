
// by defualt this causes flags because C++ automatically treats floating point numbers as type double.
// this allows for a decently high standard of precision for most numbers automatically.

// however if you have the compiler set to stop on warning this will cause the compiler to stop.
float my_addition(float x, float y) {
	return x + y;
}